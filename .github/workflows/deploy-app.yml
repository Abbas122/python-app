name: deploy-app
on:
  workflow_dispatch: 

jobs:
    deploy:
      runs-on: self-hostet
      steps:
        - name: checkout code
          run: action/checkout@v4

        - name: deloy with docker compose
          run: docker compose up -d  --build
  
